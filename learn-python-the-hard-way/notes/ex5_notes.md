String is something in the program that might go to a human 
Strings are created using ""

Embedded strings use f which os for format
you refernce the string in {} to print it as is and not as a string

syntax example:
syntaxExample = Example
print (f "Here is how you would {syntaxExample} created an embedded string")