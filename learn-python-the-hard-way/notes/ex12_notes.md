input() shows a prompt and waits for the user to type something. It always returns a string.
Example: age = input("How old are you? ")

f-strings let you insert variables into a string using {}.
Example: print(f"So, you're {age} old, {height} tall, and {weight} heavy.")

to access pydoc on MAC:
python3 -m pydoc input

pydoc is a built-in tool in Python that shows documentation about Python modules, classes, functions, and more—right from your terminal.

It works with:

Standard Python modules (like math, str, input)

Custom scripts you've written (if they're in your current directory or Python path)